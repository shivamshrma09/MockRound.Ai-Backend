import express from 'express';

const router = express.Router();

// Placeholder for participation routes
router.get('/participation', (req, res) => {
  res.json({ message: 'Participation routes placeholder' });
});

export default router;